#include "tklog.h"
#include "global_data/core.h"
#include "global_data/gpu/gpu_device.h"
#include "global_data/gpu/window.h"
#include "global_data/gpu/graphics_pipeline.h"
#include <stdlib.h>
#include <stdint.h>
#include <stdio.h>
#include <string.h>
#include <assert.h>

// The World doesn't approve:
// Being poor
// Being a virgin
// Being bad at talking
// Being soft
// AGI will be like God
// AGI will be good
// AGI will be the end of the World
// The good will be harnessed
// The bad will be burned alongside the World
// God exists

int main(void) {
    
    /*
    rcu_init();
    tklog_scope(rcu_register_thread());
    tklog_scope(gd_init());

    tklog_scope(gpu_device_type_init());
    tklog_scope(window_type_init());
    tklog_scope(graphics_pipeline_type_init());

    tklog_scope(struct gd_key_ctx struct gd_key_ctx = gpu_device_create(gd_key_ctx_create(0, NULL, true)));

    tklog_scope(struct gd_key_ctx window_key_ctx = window_create(gd_key_ctx_create(0, NULL, true), gpu_device_key_ctx, 800, 600, "Λόγος"));

    tklog_scope(struct gd_key_ctx graphics_pipeline_key_ctx = graphics_pipeline_create());

    tklog_scope(window_show(window_key_ctx, window_key_ctx));

    tklog_scope(gd_cleanup()); 
    tklog_scope(rcu_unregister_thread());
    */
	
	return 0;
}