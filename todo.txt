- add factorial function.
- add symbolic calculations with SymEngine and/or GiNaC. 
- Changing the plotting area when changing tab. 
- make tab work for indentation and also auto indent when pressing endter on lines that are indented.
- Better coloring for the syntax and for the ui in general. 
- make ctrl + '+' work so that text and buttons can become larger. 
- make the stop button actually stop the plotting.
- create a restart button.
- Ask for saving unsaved files and remember opened files when closing so that when opening you get the same files again.
- make the application pause working when the window is not visible. 
- make a box for each return so that each plot can be manage by each box on the left side and you can stop individual plots and change color on individual plots and more, as well as when writing each block can have multiple lines when clicking enter, but when a line is actually plotting something enter rather play off the box and remove the typeing cursor from the box. This means you cannot have any lines after a line the plots or prints anything. If it prints anything it will appear right under the box and the next box will be under there again. Bellow the last box is a plus sign so you can create a new box without being done with the prior box, but when youre at the last box and clicking enter causes a plot or print a new box will automatically be made as well. There should also be a transparent copy icon on the top right corner of each box to copy all the containing text and the corners of the boxes should be rounded off. Whenever the last line which plots or prints occurs, it should at least always print what ever was interpreted so that you can see if the original text was manipulated with symbolic calulations before being plotted to the render area. Sometimes you dont want to plot anything because 4+9 doesnt contain axis1 or axis2 so it should rather only be printed. Some goes with some expression containing both x and y but the answer ends up being only a number so that it does need to be plottet. 

12.02.2026
- alpha value not working. 
- event system kind of slow. feels less snappy.
- improve text box ui. e.g. empty box takes up too much space. too much space used around the boxes. color button ugly. play/pause/stop buttons should be on each box like in jupiter
- syntax suggestions
- Remove status message under each box. 
- split view dragging ugly.
- resources not cleaned up when shutting down
- Adding new box button too far down and '+' icon too small
- ratios between ui components changes when zooming in or out
- file -> Open button freezes program. Seems like all buttons under file freezes. 
- This not working: 
	f(x, y): y=x²
	f(x, y)
- Without doing anything, just existing, its consuming lots of resources
- Ask for removing cell in notebook when clicking cross
- Make a new cell be created when entering and the semantics of the code creates a plot, otherwise enter should create newline
- When defining a function in one cell and plotting it in another cell the plot gets the color of the first cell
- (x>=y and x<=y) doesnt plot the line x=y which it should. Seems like >= becomes > and <= becomes < maybe? No wait it doesn matter anyways